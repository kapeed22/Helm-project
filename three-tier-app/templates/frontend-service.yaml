apiVersion: v1
kind: Service
metadata:
  name: {{ include "three-tier.fullname" . }}-frontend-svc
  namespace: {{ .Values.global.environment }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: frontend
    env: {{ .Values.global.environment }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "three-tier.fullname" . }}-backend-svc
  namespace: {{ .Values.global.environment }}
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
  selector:
    app: backend
    env: {{ .Values.global.environment }}
